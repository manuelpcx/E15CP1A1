<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pictory</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="#">Link1</a></li>
        <li><a href="#">Link2</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <% if user.sign_in? %>
              <%= current_user.name%>
            <% else %>
              Ingresar
            <% end %>  <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if user.sign_in? %>
            <li><a href="<%= link_to "Salir", session_path(current_user), method: :delete %>">Salir</a></li>
            <% else %>
            <li><a href="<%= users_sign_in_path %>">Ingreso</a></li>
            <li><a href="<%= users_sign_up_path %>">Registrar</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
